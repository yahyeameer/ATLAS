# /etc/atlas/hermes.env  (owner root, group atlas-agent, mode 0640)
# Model provider key for the tiers in deploy/hermes/models.yaml. Nothing else
# goes here: bot tokens are per profile, broker credentials never touch the
# agent host.
ANTHROPIC_API_KEY=
