# ATLAS profile distribution. `description` is also this profile's Kanban routing
# description: deploy/hermes/bootstrap.py applies it with `hermes profile describe`.
name: strategy-researcher
version: 0.1.0
description: 'Designs trading-setup hypotheses and experiments (trend pullback, session breakout, liquidity
  sweep): writes the hypothesis before any test, picks dev or validation windows, and specifies backtests.'
hermes_requires: '>=0.21.4'
author: ATLAS
license: UNLICENSED
env_requires: []
